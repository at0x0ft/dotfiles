# auto-generated by home-manager (shell-hook)
for __hm_hook_dir in "@base_dir@"/??-*.d; do
  [ -d "${__hm_hook_dir}" ] || continue
  for __hm_hook_f in "${__hm_hook_dir}"/*; do
    [ -r "${__hm_hook_f}" ] && . "${__hm_hook_f}"
  done
done
unset __hm_hook_dir __hm_hook_f
