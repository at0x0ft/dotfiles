#!/bin/bash -e
# sudo_exec apt update
# sudo_exec apt upgrade -y
# sudo_exec apt autoremove -y
# sudo_exec apt install -y build-essential curl file git

# ln -s $(wslpath -u $(cmd.exe /c echo %USERPROFILE% 2>/dev/null) | sed 's/\r//') ~/winhome
# echo '[automount]\noptions = "metadata"' | sudo tee -a /etc/wsl.conf

# echo "" | /bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"
# test -d ~/.linuxbrew && eval $(~/.linuxbrew/bin/brew shellenv)
# test -d /home/linuxbrew/.linuxbrew && eval $(/home/linuxbrew/.linuxbrew/bin/brew shellenv)
# test -r ~/.bash_profile && echo "eval \$($(brew --prefix)/bin/brew shellenv)" >>~/.bash_profile
# echo "eval \$($(brew --prefix)/bin/brew shellenv)" >>~/.profile
# echo "eval \$($(brew --prefix)/bin/brew shellenv)" >> ~/.zshrc
# brew install gcc git zsh subversion autoconf
# echo `which zsh` | sudo tee -a /etc/shells
# chsh -s `which zsh`

# zsh

# yes | sh -c "$(curl -fsSL https://raw.githubusercontent.com/zdharma/zinit/master/doc/install.sh)"
# echo "\nsource $HOME/.zinit/plugins.zsh" >> $HOME/.zshrc
# source ~/.zshrc
# zinit self-update
# exit

# from dotfiles
# ln -s ~/winhome/Programming/dotfiles/app/zsh/rc.zsh ~/.zshrc
# ln -s `dirname $(readlink -f ~/.zshrc)`/zinit ~/.zinit
# zsh -i -c 'zinit module build; @zinit-scheduler burst || true'
