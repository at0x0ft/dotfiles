# Dotfiles Testing Dockerfiles

Dockerfiles for testing environment.

# How to test

```sh
./build.sh <path to Dockerfile from this directory> <distro version>
```

For example...

```sh
./build.sh ubuntu/Dockerfile 20.04
./build.sh debian/Dockerfile latest
```

