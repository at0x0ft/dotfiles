# Dotfiles Testing Dockerfiles

Dockerfiles for testing environment.

# How to test

```sh
./build.sh <path to Dockerfile> <distro version>
```

For example...

```sh
./build.sh desktop/ubuntu/Dockerfile 20.04
```

